import numpy as np

def program_2():
    X = np.arange(0, 10).reshape((2, 5))
    v = 3.6
    return X.flat[np.abs(X - v).argmin()]

# Виведення результату
result_2 = program_2()
print("Найближчий елемент до 3.6:", result_2)
